services:
  gateway:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: gateway
    hostname: gateway
    ports:
      - "8080:8080"
    networks:
      - ms-network

networks:
  ms-network:
    external: true